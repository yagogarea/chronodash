apiVersion: 1

# Notification policies for Grafana alerting
policies:
  - orgId: 1
    receiver: Discord
    group_by: ['alertname', 'severity']
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
